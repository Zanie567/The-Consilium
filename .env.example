# ── Database ──────────────────────────────────────────────────────────────────
# Prisma connection string. Use the Supabase "Transaction" pooler URL for
# serverless deployments, and the direct URL for migrations.
DATABASE_URL="postgresql://postgres:[password]@[host]:5432/postgres?pgbouncer=true&connection_limit=1"
# Direct (non-pooler) connection — required by Prisma CLI and the seed/migration
# scripts (db:push, db:seed, db:studio, prisma.config.ts). The deployed site does
# NOT use this; only local DB tooling does.
DIRECT_URL="postgresql://postgres:[password]@[host]:5432/postgres"

# ── Authentication (NextAuth.js) ───────────────────────────────────────────────
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here"
GOOGLE_CLIENT_ID="your-google-client-id-here"
GOOGLE_CLIENT_SECRET="your-google-client-secret-here"
# The canonical URL of the deployment. Required for OAuth redirects and email links.
NEXTAUTH_URL="https://theconsilium.co.uk"

# ── Site URL ──────────────────────────────────────────────────────────────────
# Public canonical URL used in metadata, OG images, sitemaps, and email links.
NEXT_PUBLIC_SITE_URL="https://theconsilium.co.uk"

# ── Supabase ──────────────────────────────────────────────────────────────────
# Found in: Supabase > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ..."
SUPABASE_ANON_KEY="eyJ..."
SUPABASE_SERVICE_ROLE_KEY="eyJ..."

# ── Email (Resend) ─────────────────────────────────────────────────────────────
# Found at: https://resend.com/api-keys
RESEND_API_KEY="re_..."

# ── Scheduled publishing (cron) ────────────────────────────────────────────────
# Shared secret used to authenticate calls to /api/publish-scheduled.
# Must match the secret configured in your Vercel Cron or external scheduler.
# Generate with: openssl rand -base64 32
CRON_SECRET="your-cron-secret-here"

# ── Admin ─────────────────────────────────────────────────────────────────────
# Comma-separated list of email addresses that are granted ADMIN role on first login.
ADMIN_EMAILS="admin@example.com,another@example.com"

# ── Article translation (DeepL) ───────────────────────────────────────────────
# Server-only key for reader-facing machine translation of articles into French,
# Spanish, German and Simplified Chinese. Create one at
# https://www.deepl.com/pro-api (the Free plan allows 500,000 characters/month).
#
# MUST NOT be prefixed NEXT_PUBLIC_: it is read only by server code, and a
# NEXT_PUBLIC_ copy would be inlined into the browser bundle.
#
# When this is unset the feature is simply off: the language selector is not
# rendered and every article serves as English. Nothing else changes.
#
# Verify a key before launch with:
#   node --env-file=.env.local scripts/verify-deepl.mjs
# That script also confirms every language code the site uses is one the account
# can actually translate into, and reports the remaining character allowance.
DEEPL_API_KEY=

# Optional. The API host is normally derived from the key (a ":fx" suffix means
# the free host), which is what DeepL documents. Set this only if
# scripts/verify-deepl.mjs reports that the key authenticates against the other
# host, e.g. DEEPL_API_HOST="https://api-free.deepl.com".
DEEPL_API_HOST=

# ── Economic data APIs (optional) ─────────────────────────────────────────────
# FRED (Federal Reserve Economic Data): https://fred.stlouisfed.org/docs/api/api_key.html
# Used for the macro half of the homepage ticker. Market quotes need no key.
FRED_API_KEY=""
